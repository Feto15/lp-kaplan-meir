{
  "name": "lp-kaplan-meir",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 execute $npm_package_config_d1Name --remote --file cloudflare/schema.sql"
  },
  "config": {
    "d1Name": "lp"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251121.0",
    "wrangler": "^4.50.0"
  },
  "dependencies": {
    "hono": "^4.10.6"
  }
}
